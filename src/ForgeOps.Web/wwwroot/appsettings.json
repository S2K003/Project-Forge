{
  "ForgeOps": {
    "//": "Public API base URL. Empty = Demo Mode only (no backend). Set at build time on Vercel via VITE-style env or by editing this file in the deploy.",
    "ApiBaseUrl": ""
  }
}
